# T3 — Reusable infinite-scroll grid data source

## Why

Large tables use infinite scroll through a custom data source tied to the
legacy machinery. A prototype of the replacement already works.

## What

Turn the prototype into one small, tested, reusable data source that all
infinite-scroll tables can share.

## Done when

The shared data source is merged with tests and the pilot table runs on it
in production.